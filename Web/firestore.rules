rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /app_stories/{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }

    match /story_suggestions/{document=**} {
      allow read, write: if true;
    }
  }
}
